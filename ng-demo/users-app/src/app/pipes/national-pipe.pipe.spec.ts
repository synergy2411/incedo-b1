import { NationalPipePipe } from './national-pipe.pipe';

describe('NationalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NationalPipePipe();
    expect(pipe).toBeTruthy();
  });
});
